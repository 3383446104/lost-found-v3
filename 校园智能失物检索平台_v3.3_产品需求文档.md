# 校园智能失物检索平台 — v3.3 产品需求文档 (PRD)

> **版本**：v1.0 | **日期**：2026-06-30 | **作者**：产品团队
>
> **上游文档**：[竞品分析报告](./校园智能失物检索平台_竞品分析与产品定位报告.md) | [用户角色与场景](./校园智能失物检索平台_核心用户角色与场景设计.md) | [SRS v3.2](./lost-found-v3/SRS.md)

---

## 目录

1. [版本概述](#1-版本概述)
2. [变更清单总览](#2-变更清单总览)
3. [详细需求规格](#3-详细需求规格)
4. [API 变更说明](#4-api-变更说明)
5. [前端页面变更说明](#5-前端页面变更说明)
6. [数据库变更说明](#6-数据库变更说明)
7. [验收标准](#7-验收标准)
8. [迭代计划](#8-迭代计划)

---

## 1. 版本概述

### 1.1 版本目标

v3.3 是一次**产品逻辑收敛 + 体验重构**版本，核心目标：

| 目标 | 说明 |
|------|------|
| 🔒 **安全闭环** | 去除访客角色，所有功能需注册登录后使用 |
| 🔄 **流程简化** | 认领/归还从"双方确认"改为"发布者自标记" |
| 🎨 **体验重构** | 个人中心对标 GitHub/Notion 设计，砍掉头像上传 |
| ⚡ **管理提效** | 批量审核通过 + 用户逻辑删除 |
| 📊 **统计聚焦** | 统计卡片聚焦"总计找回"，去除分类占比 |

### 1.2 与 v3.2 的关系

```
v3.2 (当前线上版本)
  │
  ├── 保留 ──→ CLIP 多模态匹配、JWT 认证、审核系统、通知系统
  ├── 修改 ──→ 角色体系、认领流程、个人中心、数据统计
  ├── 新增 ──→ 批量审核、历史记录、公告系统
  └── 删除 ──→ 访客角色、头像上传、确认/拒绝认领
         │
         ▼
      v3.3 (本版本)
```

### 1.3 关键设计决策

| 决策 | 理由 | 来源 |
|------|------|------|
| 砍掉访客 | 失物信息涉及隐私，需注册才能浏览；提升用户注册率 | 新增需求 + 竞品分析（42%用户担心隐私） |
| 砍掉头像 | 失物平台非社交产品，头像无业务价值；简化个人中心 | 新增需求 |
| 自标记替代确认 | 减少交互环节，发布者最清楚物品是否已找回 | 新增需求 |
| 总计找回替代今日找回 | 累计数字更有成就感，驱动留存 | 新增需求 |

---

## 2. 变更清单总览

### 2.1 破坏性变更 (Breaking Changes)

| 编号 | 变更项 | v3.2 | v3.3 | 影响范围 |
|:----:|--------|------|------|----------|
| 💥 B1 | 角色体系 | 访客可浏览物品列表 | 所有页面需登录 | 路由守卫、所有页面 |
| 💥 B2 | 认领流程 | 申请人发起 → 发布者确认/拒绝 | 申请人发起 → 通知含联系方式 → 发布者自标记「已找回/已认领」 | API + ItemDetailView |
| 💥 B3 | 数据统计 | 4卡片：展示中/今日失物/今日拾物/今日找回 | 4卡片：展示中/今日失物/今日拾物/总计找回 | HomeView + API |
| 💥 B4 | 个人中心 | 头像上传 + 信息展示 + 密码/用户名/联系方式 | 砍掉头像 → 信息卡片 + 分段式表单 + 历史记录 | ProfileView + API |

### 2.2 新增功能

| 编号 | 功能 | 优先级 | 说明 |
|:----:|------|:------:|------|
| ✨ N1 | 批量审核通过 | P0 | 审核列表多选 + 一键批量通过 |
| ✨ N2 | 个人信息历史记录 | P0 | 个人中心内查看自己发布/认领的历史物品 |
| ✨ N3 | 公告系统 | P1 | 管理员发布公告，全部用户可见（已实现，需整合） |
| ✨ N4 | 用户逻辑删除 | P1 | 管理员删除用户时软删除 + 关闭物品（已在 admin.py 实现） |

### 2.3 修改功能

| 编号 | 功能 | v3.2 | v3.3 |
|:----:|------|------|------|
| 🔧 M1 | 认领通知 | 不含联系方式 | 通知 + 邮件内含申请人手机号 |
| 🔧 M2 | 防重复申请 | 标题含「认领/归还」即拦截 | 改为精确匹配 user_id + item_id + is_read=0 |
| 🔧 M3 | 物品状态流转 | pending→active→claimed/done/rejected/closed | pending→active→claimed（仅此一条路径，发布者自标记触发） |
| 🔧 M4 | 驳回物品管理 | 无草稿概念 | 驳回物品在发布页以编辑模式打开，引导修改重提 |
| 🔧 M5 | 禁用用户 | toggle_status 切换 user↔disabled | 同，但 disabled 用户登录时明确返回"账号已被禁用" |

### 2.4 删除功能

| 编号 | 删除项 | v3.2 实现 | 删除理由 |
|:----:|--------|-----------|----------|
| ❌ D1 | 访客角色 | 未登录可浏览物品 | 隐私保护 + 提升注册率 |
| ❌ D2 | 头像上传 | POST /api/me/avatar | 无业务价值 |
| ❌ D3 | 确认/拒绝认领 | PUT /api/items/{id}/claim/confirm | 改为自标记，简化流程 |
| ❌ D4 | 分类占比统计 | GET /api/stats/dashboard 中的 category 占比 | 聚焦核心指标 |
| ❌ D5 | 累计活跃统计 | GET /api/stats/dashboard 中的 active 累计 | 同上 |

---

## 3. 详细需求规格

### 3.1 角色体系重构 (B1)

#### 需求描述
所有功能页面均要求登录后方可访问。未登录用户仅可见登录页和注册页。

#### 路由守卫变更

```
v3.2:
  /items      → 公开（未登录可见）
  /items/:id  → 公开（未登录可见）
  /match      → 需登录
  /publish    → 需登录

v3.3:
  /           → 未登录重定向 /login
  /login      → 仅未登录
  /register   → 仅未登录
  所有其他路由 → 需登录（requiresAuth: true）
```

#### 登录页变更
当已登录用户访问 `/login` 或 `/register` 时：
- `role=admin` → 重定向 `/admin`
- `role=user` → 重定向 `/items`

#### 禁用用户登录拦截
`POST /api/login` 增加检查：
```python
if db_user['role'] == 'disabled':
    raise HTTPException(status_code=403, detail="账号已被禁用，请联系管理员")
```

#### 验收标准
- [ ] 未登录访问 `/items` → 跳转 `/login`
- [ ] 未登录访问 `/match` → 跳转 `/login`
- [ ] 未登录访问任意非认证页 → 跳转 `/login`
- [ ] 已登录访问 `/login` → 跳转 `/items` (user) 或 `/admin` (admin)
- [ ] disabled 用户登录 → 返回 403 "账号已被禁用"

---

### 3.2 认领/归还流程重构 (B2, M1, M2, D3)

#### 需求描述
取消「确认/拒绝」两步交互，改为发布者直接标记物品状态。申请人发起认领时，通知中附带其手机号，双方自行联系。

#### 新流程图

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────┐
│ 申请人    │    │   系统        │    │  发布者       │    │  物品     │
│ (林小悦)  │    │              │    │  (陈知行)     │    │  状态     │
└────┬─────┘    └──────┬───────┘    └──────┬───────┘    └────┬─────┘
     │                 │                   │                 │
     │ 点击「我要认领」 │                   │                 │
     │────────────────→│                   │                 │
     │                 │                   │                 │
     │                 │ 发送通知+邮件      │                 │
     │                 │ (含申请人电话)     │                 │
     │                 │──────────────────→│                 │
     │                 │                   │                 │
     │  双方电话联系    │                   │                 │
     │←────────────────────────────────────→│                 │
     │                 │                   │                 │
     │  线下归还物品    │                   │                 │
     │←────────────────────────────────────→│                 │
     │                 │                   │                 │
     │                 │                   │ 点击「已找回」   │
     │                 │                   │────────────────→│
     │                 │                   │                 │ status=claimed
     │                 │                   │                 │ 不再展示于列表
     │                 │                   │                 │ 移入历史记录
     │                 │                   │                 │ 总计找回+1
     │                 │                   │                 │
     │                 │  发送感谢通知      │                 │
     │                 │──────────────────→│                 │
```

#### API 变更

**删除端点：**
```
DELETE  PUT /api/items/{id}/claim/confirm   ← 删除
```

**新增端点：**
```
PUT /api/items/{id}/mark-claimed
```

**请求/响应：**
```json
// Request (body 可为空)
{}

// Response
{
  "success": true,
  "message": "物品已标记为已找回",
  "new_status": "claimed"
}
```

**修改端点：**
```
POST /api/items/{id}/claim
```
通知内容变更——增加申请人联系方式：
```python
# 变更前
content = f"物品 '{item['title']}' 收到{action_word}申请，请及时处理"

# 变更后
content = f"物品 '{item['title']}' 收到{action_word}申请。申请人联系方式：{claimer_phone}。请及时联系确认。"
```

邮件模板同步增加联系方式展示。

#### 防重复申请逻辑 (M2)

```python
# 变更前：模糊匹配标题
cursor.execute(
    "SELECT id FROM notifications WHERE user_id = ? AND link = ? AND is_read = 0 AND (title LIKE '%认领%' OR title LIKE '%归还%')",
    (item['user_id'], f"/items/{item_id}")
)

# 变更后：精确匹配 user_id + item_id + is_read
# 通过新增字段或查询通知内容中的申请人信息来判断
cursor.execute(
    "SELECT id FROM notifications WHERE user_id = ? AND link = ? AND is_read = 0 AND content LIKE ?",
    (item['user_id'], f"/items/{item_id}", f"%用户 '{claimer['username']}'%")
)
```

#### 前端变更

**ItemDetailView：**
- 非发布者看他人物品 → 「我要认领」(lost) / 「我要归还」(found) 按钮
- 发布者看自己物品 → 「标记为已找回」(lost) / 「标记为已认领」(found) 按钮
- 已认领物品 → 显示「该物品已找回」状态标签，隐藏操作按钮

#### 验收标准
- [ ] 申请人点击认领 → 发布者收到含申请人电话的通知 + 邮件
- [ ] 发布者点击「已找回」→ 物品状态变为 claimed，从列表页消失
- [ ] 同一用户对同一物品重复申请 → 返回 400 "已发送申请"
- [ ] 发布者不能对自己的物品发起认领 → 返回 400
- [ ] 已 claimed 物品不可再操作 → 返回 400

---

### 3.3 个人中心重构 (B4, D2)

#### 需求描述
砍掉头像上传功能，对标 GitHub Settings / Notion Settings 的信息架构，重新设计个人中心页面。

#### 信息架构

```
ProfileView
├── 📋 个人信息卡片（顶部横幅）
│   ├── 首字母头像（不可点击，纯展示）
│   ├── 用户名 + 角色标签
│   ├── 用户 ID
│   └── 注册时间
│
├── ⚙️ 设置区域（Tab 切换 or 分段式）
│   ├── Tab 1: 账户设置
│   │   ├── 修改用户名（inline edit）
│   │   └── 修改密码（inline edit）
│   │
│   ├── Tab 2: 联系方式
│   │   ├── 手机号（inline edit）
│   │   └── 邮箱（inline edit）
│   │
│   └── Tab 3: 历史记录 ← 新增
│       ├── 我发布的物品（失物 + 拾物）
│       └── 我参与的认领（作为申请人的记录）
│
└── ⚠️ 危险区域
    └── 注销账号（红色警告卡片，需二次确认）
```

#### 设计要点（对标 GitHub Settings）

| 元素 | GitHub Settings 特征 | 我们的方案 |
|------|---------------------|-----------|
| 信息层级 | 左侧导航 + 右侧内容区 | 移动端：单列；桌面端：Tab 切换 |
| 编辑模式 | Inline edit（点击 → 输入框 → 保存/取消） | 同：行内编辑 + 确认/取消按钮 |
| 危险操作 | 页面底部红色区域，需二次确认 | 页面底部 Danger Zone 卡片 |
| 间距 | 宽松呼吸感 | 沿用 4px 网格，section 间距 32px |

#### API 变更

**删除端点：**
```
DELETE  POST /api/me/avatar   ← 删除
DELETE  GET /api/avatars/{filename}  ← 删除
```

**修改端点 (GET /api/me)：**
响应体移除 `avatar_path` 字段。

**新增端点：**
```
GET /api/me/history
```
返回当前用户的所有物品（含已认领、已关闭）及认领记录。

```json
// Response
{
  "success": true,
  "data": {
    "my_items": [
      {
        "id": 1,
        "type": "lost",
        "title": "白色 AirPods Pro",
        "status": "claimed",
        "category": "电子产品",
        "created_at": "2026-06-28 14:30",
        "image_path": "..."
      }
    ],
    "my_claims": [
      {
        "id": 5,
        "item_id": 10,
        "item_title": "捡到黑色钱包",
        "item_type": "found",
        "claimed_at": "2026-06-29 09:15"
      }
    ]
  }
}
```

#### 验收标准
- [ ] 个人中心不再显示头像上传区域
- [ ] 首字母头像替代原头像位置（纯展示，无编辑功能）
- [ ] "历史记录" Tab 可见且可查看自己发布/认领的物品
- [ ] 用户名/密码/联系方式支持 inline edit
- [ ] 注销按钮在页面底部 Danger Zone 区域
- [ ] 桌面端和移动端均可用

---

### 3.4 批量审核通过 (N1)

#### 需求描述
审核列表支持多选，一键批量审核通过。驳回不支持批量操作（需逐一填写驳回理由）。

#### UI 方案

```
┌─────────────────────────────────────────────────────┐
│  物品审核                        [23 条待审]         │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ ☐ 全选     [一键批量通过]  [导出列表]        │    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  ┌──────────────────────────────────────────────┐   │
│  │ ☐ │ ID │ 标题 │ 类型 │ 分类 │ 时间 │ 操作    │   │
│  │───│────│──────│──────│──────│──────│─────────│   │
│  │ ☑ │ 42 │ Air..│ 失物 │ 电子 │ 14:30│ 通过 驳回│   │
│  │ ☑ │ 41 │ 钱包 │ 拾物 │ 证件 │ 14:25│ 通过 驳回│   │
│  │ ☐ │ 40 │ 钥匙 │ 拾物 │ 其他 │ 14:20│ 通过 驳回│   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

#### API 变更

**新增端点：**
```
POST /api/admin/reviews/batch-approve
```

```json
// Request
{
  "item_ids": [42, 41, 39, 38]
}

// Response
{
  "success": true,
  "message": "已批量通过 4 件物品，匹配通知已发送",
  "approved_count": 4,
  "failed_ids": []
}
```

后端处理逻辑：
```python
@router.post("/reviews/batch-approve")
async def batch_approve(payload: dict, background_tasks: BackgroundTasks, ...):
    item_ids = payload.get("item_ids", [])
    approved = 0
    failed = []
    for item_id in item_ids:
        try:
            # 复用单条审核逻辑
            with get_db_connection() as conn:
                cursor = conn.cursor()
                cursor.execute("SELECT type, review_status FROM items WHERE id = ?", (item_id,))
                row = cursor.fetchone()
                if not row or row["review_status"] != "pending":
                    failed.append(item_id)
                    continue
                cursor.execute("""UPDATE items SET review_status='approved', reviewer_id=?, review_time=CURRENT_TIMESTAMP, status='active' WHERE id=? AND review_status='pending'""", (admin_id, item_id))
                if cursor.rowcount > 0:
                    approved += 1
                    background_tasks.add_task(auto_match_and_notify, item_id, row["type"])
        except Exception:
            failed.append(item_id)
    return {"success": True, "approved_count": approved, "failed_ids": failed}
```

#### 验收标准
- [ ] 审核列表表格首列增加复选框（含全选）
- [ ] 选中 ≥ 1 条时，「一键批量通过」按钮可用
- [ ] 点击「一键批量通过」→ 二次确认弹窗 → 执行
- [ ] 执行完成后显示 "已通过 N 件，失败 M 件"
- [ ] 批量通过后列表自动刷新
- [ ] 驳回按钮在批量模式下不可用（不选或选中的不显示批量驳回）

---

### 3.5 数据统计重构 (B3, D4, D5)

#### 需求描述
统计卡片从「展示中/今日失物/今日拾物/今日找回」改为「展示中/今日失物/今日拾物/总计找回」。去除分类占比和累计活跃数据。

#### 变更对比

| 卡片位置 | v3.2 | v3.3 |
|:--------:|------|------|
| 卡片 1 | 📦 展示中 (active 数量) | 📦 展示中 (不变) |
| 卡片 2 | 📝 今日失物 | 📝 今日失物 (不变) |
| 卡片 3 | ✅ 今日拾物 | ✅ 今日拾物 (不变) |
| 卡片 4 | 🎯 今日找回 | 🏆 **总计找回** |

#### 总计找回计数逻辑

```python
# 查询 items 表中 status='claimed' 的总数
cursor.execute("SELECT COUNT(*) as total FROM items WHERE status = 'claimed'")
```

每次用户点击「已找回」/「已认领」时，`status` 变为 `claimed`，总计找回数自动 +1。

#### API 变更

**修改端点：**
```
GET /api/stats/dashboard
```

```json
// v3.3 响应
{
  "success": true,
  "data": {
    "active_count": 45,       // 不变
    "today_lost": 8,           // 不变
    "today_found": 15,         // 不变
    "total_claimed": 342       // 新：总计找回（替代 today_claimed）
  }
  // 移除：category_stats, total_active
}
```

#### 验收标准
- [ ] 首页第 4 张卡片显示「总计找回」+ 累计数字
- [ ] 每次标记 claimed 后数字自动 +1
- [ ] 分类占比不再展示
- [ ] 累计活跃不再展示

---

### 3.6 历史记录 (N2)

#### 需求描述
个人中心新增历史记录模块，展示用户发布过的所有物品（含已认领/已关闭/已驳回）和参与过的认领记录。

#### 前端设计

```
┌─────────────────────────────────────────────────────┐
│  历史记录                                           │
│                                                     │
│  ┌─ Tab: 我发布的 ──┬── Tab: 我认领的 ─┐            │
│                                                     │
│  ┌──────────────────────────────────────────────┐   │
│  │ 📷 白色 AirPods Pro                           │   │
│  │    失物 · 电子产品 · 2026-06-28               │   │
│  │    [已找回] ← 状态标签                        │   │
│  │                                               │   │
│  │ 📷 黑色双肩包                                  │   │
│  │    拾物 · 包袋箱包 · 2026-06-20               │   │
│  │    [已认领] ← 状态标签                        │   │
│  │                                               │   │
│  │ 📄 丢失校园卡（无图片）                        │   │
│  │    失物 · 证件卡片 · 2026-06-15               │   │
│  │    [已驳回] 理由：照片不清晰                  │   │
│  └──────────────────────────────────────────────┘   │
│                                                     │
│  （空状态）暂无记录 → el-empty + "去发布" CTA       │
└─────────────────────────────────────────────────────┘
```

#### 验收标准
- [ ] 个人中心可切换至「历史记录」Tab
- [ ] 展示自己发布的所有物品（不限状态）+ 状态标签
- [ ] 展示自己参与认领的记录
- [ ] 空状态有引导设计
- [ ] 点击历史记录条目可跳转物品详情

---

### 3.7 公告系统整合 (N3)

公告系统已基本实现（`announcements.py` + `announcements.js`），v3.3 需整合到首页：

#### 整合位置
首页物品列表上方，以横向公告栏形式展示置顶公告：

```
┌─────────────────────────────────────────────────────┐
│  📢 图书馆自习区近期遗失物品较多，请同学们离开时...   │
│     2026-06-30 · 管理员                              │
└─────────────────────────────────────────────────────┘
```

#### 验收标准
- [ ] 首页展示置顶公告（如有）
- [ ] 仅已登录用户可见公告
- [ ] 公告栏可折叠/展开

---

## 4. API 变更说明

### 4.1 新增端点

| 方法 | 路径 | 认证 | 说明 |
|------|------|:----:|------|
| POST | `/api/admin/reviews/batch-approve` | Admin | 批量审核通过 |
| PUT | `/api/items/{id}/mark-claimed` | ✅ | 发布者标记物品已找回/已认领 |
| GET | `/api/me/history` | ✅ | 获取个人物品历史记录 |

### 4.2 修改端点

| 方法 | 路径 | 变更说明 |
|------|------|----------|
| POST | `/api/login` | 增加 disabled 用户拦截（403） |
| GET | `/api/me` | 响应移除 `avatar_path` |
| PUT | `/api/me` | 移除 `avatar_path` 更新逻辑 |
| POST | `/api/items/{id}/claim` | 通知内容增加申请人手机号；防重复逻辑改为精确匹配 |
| GET | `/api/stats/dashboard` | 响应：`total_claimed` 替代 `today_claimed`；移除 `category_stats` 和 `total_active` |

### 4.3 删除端点

| 方法 | 路径 | 原功能 |
|------|------|--------|
| PUT | `/api/items/{id}/claim/confirm` | 确认/拒绝认领 |
| POST | `/api/me/avatar` | 上传头像 |
| GET | `/api/avatars/{filename}` | 访问头像 |

---

## 5. 前端页面变更说明

### 5.1 路由守卫变更

```diff
  router.beforeEach:
-   未登录 → 可访问 /items, /items/:id, /match
+   未登录 → 仅可访问 /login, /register
+   已登录 + /login → redirect /items (user) 或 /admin (admin)
```

### 5.2 页面变更汇总

| 页面 | 变更类型 | 说明 |
|------|:--------:|------|
| [LoginView.vue](lost-found-frontend/src/views/LoginView.vue) | 修改 | 已登录自动跳转 |
| [RegisterView.vue](lost-found-frontend/src/views/RegisterView.vue) | 修改 | 已登录自动跳转 |
| [HomeView.vue](lost-found-frontend/src/views/HomeView.vue) | 修改 | 统计卡片第4项改为总计找回；新增公告栏 |
| [ItemDetailView.vue](lost-found-frontend/src/views/ItemDetailView.vue) | **重写操作区** | 按钮逻辑改为自标记；移除确认/拒绝 UI |
| [ProfileView.vue](lost-found-frontend/src/views/ProfileView.vue) | **重写** | 砍头像 + Tab分段 + 历史记录 |
| [PublishView.vue](lost-found-frontend/src/views/PublishView.vue) | 修改 | 驳回物品编辑模式优化；草稿概念引入 |
| [AdminView.vue](lost-found-frontend/src/views/AdminView.vue) | 修改 | 批量审核功能 |
| [MatchView.vue](lost-found-frontend/src/views/MatchView.vue) | 无变更 | — |
| [NotificationsView.vue](lost-found-frontend/src/views/NotificationsView.vue) | 轻微修改 | 通知内容展示申请人联系方式 |

---

## 6. 数据库变更说明

### 6.1 无 Schema 变更

v3.3 不涉及数据库表结构修改。所有变更均为逻辑层修改。

### 6.2 数据迁移说明

| 项目 | 说明 |
|------|------|
| `avatar_path` 字段 | 保留在 `users` 表中（不删列），仅 API 不再返回/更新 |
| `status='claimed'` 的已有数据 | 计入总计找回数 |
| 已注销用户的 `avatar_path` | 文件可清理（非必须） |

---

## 7. 验收标准

### 7.1 功能验收清单

| # | 验收项 | 关联需求 | 状态 |
|:--|--------|:--------:|:----:|
| 1 | 未登录无法访问任何功能页（除登录/注册） | B1 | ⬜ |
| 2 | 禁用用户登录返回 403 | B1 | ⬜ |
| 3 | 已登录用户访问 /login 自动跳转 | B1 | ⬜ |
| 4 | 认领通知含申请人手机号 | M1 | ⬜ |
| 5 | 认领邮件含申请人手机号 | M1 | ⬜ |
| 6 | 防重复申请精确匹配 | M2 | ⬜ |
| 7 | 发布者「已找回」按钮可用，标记后状态变 claimed | B2 | ⬜ |
| 8 | 已 claimed 物品不再出现在物品列表 | B2 | ⬜ |
| 9 | 已 claimed 物品出现在历史记录 | N2 | ⬜ |
| 10 | 确认/拒绝 API 已移除 | D3 | ⬜ |
| 11 | 个人中心无头像上传功能 | D2 | ⬜ |
| 12 | 个人中心有历史记录模块 | N2 | ⬜ |
| 13 | 审核列表可多选 + 一键批量通过 | N1 | ⬜ |
| 14 | 第4张统计卡片为「总计找回」 | B3 | ⬜ |
| 15 | 分类占比和累计活跃不再展示 | D4/D5 | ⬜ |
| 16 | 首页展示置顶公告 | N3 | ⬜ |

### 7.2 非功能验收

| # | 验收项 | 标准 |
|:--|--------|------|
| 1 | 所有现有测试通过 | 0 失败 |
| 2 | 前端构建零错误 | npm run build 成功 |
| 3 | 移动端响应式正常 | 三级断点无布局破损 |
| 4 | 深色模式无异常 | 所有改动的页面深色模式下可读 |

---

## 8. 迭代计划

### 8.1 Sprint 拆分建议

| Sprint | 内容 | 预估工时 |
|:------:|------|:--------:|
| **Sprint 1** (后端) | B1 角色重构 + B2 认领流程 + M1/M2 + N1 批量审核 | 3d |
| **Sprint 2** (后端) | B3 统计 + N2 历史记录 API + D2/D3/D4/D5 删除清理 | 2d |
| **Sprint 3** (前端) | B1 路由守卫 + ProfileView 重写 + HomeView 统计变更 | 3d |
| **Sprint 4** (前端) | ItemDetailView 操作区重写 + AdminView 批量审核 UI + 公告整合 | 3d |
| **Sprint 5** (联调测试) | 前后端联调 + 全链路测试 + 文档更新 | 2d |

### 8.2 风险提示

| 风险 | 等级 | 缓解措施 |
|------|:----:|----------|
| 认领流程改动影响已有通知数据的兼容性 | 🟡 | 已存在的通知保留，仅新生成的通知走新逻辑 |
| 批量审核并发冲突（同一物品被两个管理员同时审核） | 🟡 | 沿用现有条件更新 + rowcount 检查机制 |
| ProfileView 重写可能引入回归 | 🟡 | 保留旧组件备份，新组件独立开发 |

---

> **文档维护者**：产品团队 | **最后更新**：2026-06-30
>
> **关联文档**：
> - [竞品分析与产品定位报告](./校园智能失物检索平台_竞品分析与产品定位报告.md)
> - [核心用户角色与场景设计](./校园智能失物检索平台_核心用户角色与场景设计.md)
> - [SRS v3.2](./lost-found-v3/SRS.md)
> - [项目文档 v3.2](./lost-found-v3/PROJECT.md)
